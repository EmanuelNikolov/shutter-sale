{% extends 'default/form.html.twig' %}

{% block form %}
    <h1>Edit Camera</h1>

    {{ form(edit_form) }}

    <div>
        <div>
            <a href="{{ path('camera_index') }}">Back to the list</a>
        </div>
        <div>
            {{ form(delete_form) }}
        </div>
    </div>
{% endblock %}
